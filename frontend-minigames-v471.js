'use strict';

const tuneBase = require('./frontend-minigames-v47');

function mustReplace(source, needle, replacement, label) {
  if (!source.includes(needle)) throw new Error(`v4.7.1 Audio-Patch fehlt: ${label}`);
  return source.replace(needle, replacement);
}

module.exports = function tuneV471(html) {
  html = tuneBase(html);
  html = html.replace(/songAudioUnlocked=false;lastSongSyncKey=null;/g, 'lastSongSyncKey=null;');
  html = mustReplace(html, '</style>', `.sound-prime{margin:12px 0 4px;padding:13px;border:1px solid rgba(184,255,74,.22);border-radius:16px;background:rgba(184,255,74,.055)}.sound-prime-title{font-weight:950;margin-bottom:4px}.sound-prime-text{color:var(--muted);font-size:11px;line-height:1.45;margin-bottom:9px}.sound-prime .btn.ready{background:var(--accent);color:#11170a}.sound-prime .btn.done{background:rgba(87,227,154,.12);color:#91e9b8;border:1px solid rgba(87,227,154,.22)}</style>`, 'sound styles');
  const anchor = "try{joined=JSON.parse(localStorage.getItem(KEY)||'null');}catch(e){joined=null;}";
  const extension = String.raw`
var AUDIO_WARMUP={videoId:'a47Y1lCRHlM',startSeconds:0},persistentSongVideoId=null;
function ensureGlobalYTSlot(){var wrap=document.getElementById('globalYTWrap');if(!wrap){wrap=document.createElement('div');wrap.id='globalYTWrap';wrap.className='yt-stealth';wrap.setAttribute('aria-hidden','true');var slot=document.createElement('div');slot.id='globalYTPlayer';wrap.appendChild(slot);document.body.appendChild(wrap);}return document.getElementById('globalYTPlayer');}
function updateLobbySoundButton(){var b=document.getElementById('lobbySound');if(!b)return;if(songAudioUnlocked){b.disabled=true;b.className='btn done';b.textContent='✓ Spielton aktiviert';return;}b.disabled=!ytPlayerReady;b.className='btn '+(ytPlayerReady?'ready':'secondary');b.textContent=ytPlayerReady?'🔊 Spielton aktivieren':'🔊 Spielton wird geladen …';}
cleanupYouTube=function(){if(syncTimer){clearTimeout(syncTimer);syncTimer=null;}pendingSync=null;lastSongSyncKey=null;try{if(ytPlayer&&ytPlayer.pauseVideo)ytPlayer.pauseVideo();}catch(e){}};
createYTPlayer=function(cur){cur=cur||pendingSong||AUDIO_WARMUP;pendingSong=cur;ensureGlobalYTSlot();if(ytPlayer){if(ytPlayerReady&&cur&&cur.videoId&&persistentSongVideoId!==cur.videoId){try{ytPlayer.cueVideoById({videoId:cur.videoId,startSeconds:Number(cur.startSeconds||0)});persistentSongVideoId=cur.videoId;}catch(e){}}updateLobbySoundButton();return;}try{ytPlayer=new YT.Player('globalYTPlayer',{height:'240',width:'240',videoId:cur.videoId,playerVars:{controls:0,playsinline:1,autoplay:0,rel:0,origin:location.origin},events:{onReady:function(){ytPlayerReady=true;persistentSongVideoId=cur.videoId;try{ytPlayer.cueVideoById({videoId:cur.videoId,startSeconds:Number(cur.startSeconds||0)});}catch(e){}updateLobbySoundButton();var pb=document.getElementById('songPlay');if(pb){pb.disabled=false;pb.textContent='▶ Song starten';}var ub=document.getElementById('unlockSong');if(ub){ub.disabled=false;ub.textContent=songAudioUnlocked?'✓ Ton aktiviert':'🔊 Ton aktivieren';}if(pendingSync){var p=pendingSync;pendingSync=null;playSyncedSong(p,true);}},onError:function(){ytPlayerReady=false;updateLobbySoundButton();toast('YouTube-Ton konnte nicht vorbereitet werden.');}}});}catch(e){ytPlayerReady=false;updateLobbySoundButton();}};
function prepareLobbyAudio(){if(!state||state.phase!=='lobby'||(state.selectedCats||[]).indexOf('song')<0)return;ensureGlobalYTSlot();ensureYTApi(AUDIO_WARMUP);updateLobbySoundButton();}
function activateLobbyAudio(){if(songAudioUnlocked){updateLobbySoundButton();return;}if(!ytPlayer||!ytPlayerReady){toast('Spielton wird noch geladen …');prepareLobbyAudio();return;}try{if(persistentSongVideoId!==AUDIO_WARMUP.videoId){ytPlayer.cueVideoById({videoId:AUDIO_WARMUP.videoId,startSeconds:0});persistentSongVideoId=AUDIO_WARMUP.videoId;}if(ytPlayer.unMute)ytPlayer.unMute();if(ytPlayer.setVolume)ytPlayer.setVolume(1);if(ytPlayer.seekTo)ytPlayer.seekTo(0,true);if(ytPlayer.playVideo)ytPlayer.playVideo();songAudioUnlocked=true;updateLobbySoundButton();setTimeout(function(){try{if(ytPlayer&&ytPlayer.pauseVideo)ytPlayer.pauseVideo();if(ytPlayer&&ytPlayer.setVolume)ytPlayer.setVolume(100);}catch(e){}},180);}catch(e){songAudioUnlocked=false;updateLobbySoundButton();toast('Ton konnte nicht aktiviert werden.');}}
var v471LobbyHtml=lobbyHtml;lobbyHtml=function(){var out=v471LobbyHtml();if(!state||(state.selectedCats||[]).indexOf('song')<0)return out;var box='<div class="sound-prime"><div class="sound-prime-title">🔊 Spielton</div><div class="sound-prime-text">Einmal auf diesem Handy aktivieren. Danach startet der Master die Songrunden für alle – du musst in den Songrunden nichts mehr freigeben.</div><button id="lobbySound" class="btn '+(songAudioUnlocked?'done':'secondary')+'" '+(songAudioUnlocked?'disabled':'')+'>'+(songAudioUnlocked?'✓ Spielton aktiviert':'🔊 Spielton wird geladen …')+'</button></div>';return out.replace('<div class="section">Kategorien</div>',box+'<div class="section">Kategorien</div>');};
var v471Bind=bind;bind=function(){v471Bind();var b=document.getElementById('lobbySound');if(b){b.onclick=activateLobbyAudio;updateLobbySoundButton();}};
playSyncedSong=function(m,force){var cur=state&&state.current;if(!cur||cur.type!=='song')return;var syncKey=String(state.round)+':'+String(Number(m.at||0));if(lastSongSyncKey===syncKey&&!force)return;if(!ytPlayer||!ytPlayerReady){pendingSync=m;ensureYTApi(cur);return;}lastSongSyncKey=syncKey;if(syncTimer){clearTimeout(syncTimer);syncTimer=null;}var delay=Math.max(0,Number(m.at||Date.now())-Date.now());syncTimer=setTimeout(function(){if(!state||!state.current||state.current.type!=='song')return;var late=Math.max(0,(Date.now()-Number(m.at||Date.now()))/1000),start=Number(m.startSeconds||0)+late;try{if(ytPlayer.unMute)ytPlayer.unMute();if(ytPlayer.setVolume)ytPlayer.setVolume(100);if(persistentSongVideoId!==m.videoId&&m.videoId){ytPlayer.loadVideoById({videoId:m.videoId,startSeconds:start});persistentSongVideoId=m.videoId;}else{if(ytPlayer.seekTo)ytPlayer.seekTo(start,true);if(ytPlayer.playVideo)ytPlayer.playVideo();}}catch(e){toast('Song konnte auf diesem Gerät nicht gestartet werden.');}},delay);};
playSongButton=function(){var cur=state&&state.current;if(!cur||cur.type!=='song'||!cur.isHost)return;if(songAudioUnlocked){send({t:'songPlay'});return;}unlockSongAudio(function(){send({t:'songPlay'});});};
var v471Render=render;render=function(){v471Render();if(state&&state.phase==='lobby'&&(state.selectedCats||[]).indexOf('song')>=0)setTimeout(prepareLobbyAudio,0);if(state&&state.phase==='question'&&state.current&&state.current.type==='song'){ensureYTApi(state.current);if(songAudioUnlocked){var ub=document.getElementById('unlockSong');if(ub){ub.style.display='none';var n=ub.previousElementSibling;if(n&&n.classList&&n.classList.contains('note'))n.innerHTML='✓ Spielton wurde bereits in der Lobby aktiviert. Der Master startet den Song.';}}}};
`;
  html = mustReplace(html, anchor, extension + '\n' + anchor, 'persistent lobby audio');
  return html;
};
