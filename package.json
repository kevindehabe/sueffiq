{
  "name": "sueffiq",
  "version": "2.0.0",
  "private": true,
  "description": "SüffIQ v2 – serverseitiges Multiplayer-Partyspiel ohne Anmeldung",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "test": "node --test",
    "check": "node -c server.js && node -c logic.js && node -c questions.js && node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
