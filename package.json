{
  "name": "sueffiq",
  "version": "4.0.0",
  "private": true,
  "description": "SüffIQ v4 – Multiplayer-Partyspiel mit YouTube-Songrunden ohne Anmeldung",
  "main": "server-v4.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server-v4.js",
    "test": "node --test",
    "check": "node -c server-v4.js && node -c songs.js && node -c extras.js && node -c logic.js && node -c questions.js && node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
