{
  "name": "sueffiq",
  "version": "4.5.0",
  "private": true,
  "description": "SüffIQ v4.5 – browser-first Multiplayer-Partyspiel mit einem konsolidierten Production-Server",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "test": "node --test --test-concurrency=1",
    "check": "node -c server.js && node -c server-v3.js && node -c frontend-v45.js && node -c pwa-v45.js && node -c more.js && node -c more-base.js && node -c party-extra.js && node -c songs.js && node -c extras.js && node -c logic.js && node -c questions.js && node --test --test-concurrency=1"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
